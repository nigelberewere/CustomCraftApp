class PersonalAttendance {
  DateTime date;
  bool isPresent;
  int personalWorkerKey;

  PersonalAttendance({
    required this.date,
    required this.isPresent,
    required this.personalWorkerKey,
  });
}
